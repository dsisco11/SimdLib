# Debugger visualizers

`SimdLib.natvis` provides Visual Studio views for root `SimdVector` values and
root `uint128_t`. `Api` is a stateless operation facade and therefore has no
runtime value to visualize.
