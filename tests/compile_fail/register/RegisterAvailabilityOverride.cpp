#define SIMDLIB_REGISTER_INTERFACE_AVAILABLE 1
#include <SimdLib/Config.h>

/** @brief Provides an entry point when a computed-availability override unexpectedly compiles. */
int main()
{
	return 0;
}
