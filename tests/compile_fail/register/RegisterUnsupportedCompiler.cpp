#define SIMDLIB_REQUIRE_REGISTER_INTERFACE 1
#include <SimdLib/Register.h>

/** @brief Provides an entry point when an unsupported Register compiler unexpectedly compiles. */
int main()
{
	return 0;
}
