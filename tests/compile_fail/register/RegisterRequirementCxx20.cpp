#define SIMDLIB_REQUIRE_REGISTER_INTERFACE 1
#include <SimdLib/Config.h>

/** @brief Provides an entry point when an unavailable Register requirement unexpectedly compiles. */
int main()
{
	return 0;
}
