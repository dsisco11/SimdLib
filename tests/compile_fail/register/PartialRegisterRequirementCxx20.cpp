#define SIMDLIB_REQUIRE_REGISTER_INTERFACE 1
#include <SimdLib/PartialRegister.h>

/** @brief Provides an entry point when an unavailable PartialRegister requirement unexpectedly compiles. */
int main()
{
	return 0;
}
